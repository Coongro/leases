/**
 * Vencimientos — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `vencimientos.view.ts`,
 * datos en `use-vencimientos.ts`, lógica custom en `handlers.ts`.
 */
export { VencimientosView } from './vencimientos.view.js';
