/**
 * Rescindir contrato — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `rescindir-contrato.view.ts`,
 * datos en `use-rescindir-contrato.ts`, lógica custom en `handlers.ts`.
 */
export { RescindirContratoView } from './rescindir-contrato.view.js';
