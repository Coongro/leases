/**
 * Actualizaciones — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `actualizaciones.view.ts`,
 * datos en `use-actualizaciones.ts`, lógica custom en `handlers.ts`.
 */
export { ActualizacionesView } from './actualizaciones.view.js';
