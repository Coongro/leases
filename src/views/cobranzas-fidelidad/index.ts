/**
 * Cobranzas — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `cobranzas-fidelidad.view.ts`,
 * datos en `use-cobranzas-fidelidad.ts`, lógica custom en `handlers.ts`.
 */
export { CobranzasFidelidadView } from './cobranzas-fidelidad.view.js';
