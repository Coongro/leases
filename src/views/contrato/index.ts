/**
 * Contrato — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `contrato.view.ts`,
 * datos en `use-contrato.ts`, lógica custom en `handlers.ts`.
 */
export { ContratoView } from './contrato.view.js';
