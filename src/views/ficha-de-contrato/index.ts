/**
 * Ficha de contrato — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `ficha-de-contrato.view.ts`,
 * datos en `use-ficha-de-contrato.ts`, lógica custom en `handlers.ts`.
 */
export { FichaDeContratoView } from './ficha-de-contrato.view.js';
