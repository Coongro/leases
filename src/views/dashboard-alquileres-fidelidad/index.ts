/**
 * Panel de alquileres — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `dashboard-alquileres-fidelidad.view.ts`,
 * datos en `use-dashboard-alquileres-fidelidad.ts`, lógica custom en `handlers.ts`.
 */
export { DashboardAlquileresFidelidadView } from './dashboard-alquileres-fidelidad.view.js';
