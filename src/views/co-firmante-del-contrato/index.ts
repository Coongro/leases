/**
 * Co-firmante del contrato — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `co-firmante-del-contrato.view.ts`,
 * datos en `use-co-firmante-del-contrato.ts`, lógica custom en `handlers.ts`.
 */
export { CoFirmanteDelContratoView } from './co-firmante-del-contrato.view.js';
