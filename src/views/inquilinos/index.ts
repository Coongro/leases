/**
 * Inquilinos — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `inquilinos.view.ts`,
 * datos en `use-inquilinos.ts`, lógica custom en `handlers.ts`.
 */
export { InquilinosView } from './inquilinos.view.js';
