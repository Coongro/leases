/**
 * Concepto del contrato — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `concepto-del-contrato.view.ts`,
 * datos en `use-concepto-del-contrato.ts`, lógica custom en `handlers.ts`.
 */
export { ConceptoDelContratoView } from './concepto-del-contrato.view.js';
