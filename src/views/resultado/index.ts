/**
 * Resultado — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `resultado.view.ts`,
 * datos en `use-resultado.ts`, lógica custom en `handlers.ts`.
 */
export { ResultadoView } from './resultado.view.js';
