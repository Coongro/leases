/**
 * Contratos — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `contratos.view.ts`,
 * datos en `use-contratos.ts`, lógica custom en `handlers.ts`.
 */
export { ContratosView } from './contratos.view.js';
